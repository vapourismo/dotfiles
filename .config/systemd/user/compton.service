[Unit]
Description=Compton Compositor

[Service]
Environment=DISPLAY=:0
ExecStart=/usr/bin/compton -cC --vsync opengl --refresh-rate 60 --config ${HOME}/.compton.conf

[Install]
WantedBy=default.target
